<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cart</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.1.3/css/bootstrap.min.css">
    <link rel="stylesheet" href="/Views/Cart/cart.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.1.3/js/bootstrap.bundle.min.js"></script>
    <%- include('../Template/navbar') %>
</head>

<body>
    <div class="cart-container">
        <h2>Cart</h2>
        <div class="cart">
            <table>
                <thead>
                    <tr>
                        <th>Image</th>
                        <th>Product</th>
                        <th>Price</th>
                        <th id="qua">Quantity</th>
                        <th>Total</th>
                        <th id="qua">Detail</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (typeof cart !=='undefined' && cart.length> 0) { %>
                        <% for (let i=0; i < cart.length; i++) { %>
                            <tr>
                                <td><img src="/Asset/Product/<%= cart[i].image || 'dummy.jpg' %>"
                                        alt="<%= cart[i].name %>" style="width: 50px; height: 50px;"></td>
                                <td>
                                    <%= cart[i].name %>
                                </td>
                                <td>฿<%= cart[i].price %>
                                </td>
                                <td class="quantity" id="qua">
                                    <a href="/cart/remove/<%= cart[i].order_id %>?userId=<%= userId %>"
                                        class="btnt">-</a>
                                    <%= cart[i].quantity %>
                                        <a href="/cart/add/<%= cart[i].product_id %>?userId=<%= userId %>"
                                            class="btnt">+</a>
                                </td>
                                <td>฿<%= cart[i].price * cart[i].quantity %>
                                </td>
                                <td style="width: 8%;"><a href="/product/<%= cart[i].product_id %>"
                                        class="btnt">Detail</a></td>
                            </tr>
                            <% } %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="6" class="text-center">
                                            <%= typeof message !=='undefined' && message ? message
                                                : "Your cart is empty" %>
                                        </td>
                                    </tr>
                                    <% } %>
                </tbody>
            </table>
        </div>
        <div class="total">
            <h2>Total: <%= total %>
            </h2>
        </div>
        <div class="actions">
            <a href="/">Continue Shopping</a>
            <a href="/checkout">Checkout</a>
        </div>
    </div>
    <script src="/Views/Cart/cart.js"></script>
</body>

</html>