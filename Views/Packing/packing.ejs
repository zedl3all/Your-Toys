<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Packing Management</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/Views/Packing/packing.css">
    <%- include('../Template/navbar') %>
</head>

<body>
    <div class="container">
        <div class="page-header">
            <h1><i class="fas fa-box-open"></i> Order Processing System</h1>
        </div>
        
        <div class="table-container">
            <h2><i class="fas fa-clipboard-list"></i> Order List</h2>
            <table>
                <thead>
                    <tr>
                        <th><i class="fas fa-hashtag"></i> Order ID</th>
                        <th title="Customer Name"><i class="fas fa-user"></i> Customer</th>
                        <th><i class="fas fa-info-circle"></i> Description</th>
                        <th><i class="fas fa-calendar"></i> Date</th>
                        <th><i class="fas fa-file-invoice"></i> Image(Bill)</th>
                        <th><i class="fas fa-tasks"></i> Status</th>
                        <th><i class="fas fa-tools"></i> Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- Example product rows -->
                    <% orders.forEach(order => { %>
                    <tr>
                        <td><%=order.order_id%></td>
                        <td><%=order.customer%></td>
                        <td><%=order.description%></td>
                        <td><%=order.order_date%></td>
                        <td>
                            <a href="#" class="btn-image" onclick="viewImage('/Asset/Payment/<%=order.image%>')">
                                <i class="fas fa-eye"></i> View
                            </a>
                        </td>
                        
                        <%if (order.status == 3){%>
                            <td id="status-<%=order.order_id%>" class="status-packing">Packing</td>
                        <%} else if (order.status == 4){%>
                            <td id="status-<%=order.order_id%>" class="status-success">Sucess</td>
                        <%} else if (order.status == 5){%>
                            <td id="status-<%=order.order_id%>" class="status-failed">Failed</td>
                        <%}else{%>
                            <td id="status-<%=order.order_id%>" class="status-waiting">Waiting for packing</td>
                        <%}%>   
                        <td>
                            <div class="action-buttons">
                                <a href="#" class="btn-packing" onclick="changeStatus('status-<%=order.order_id%>', 'Packing')">
                                    <i class="fas fa-box"></i> Packing
                                </a>
                                <a href="#" class="btn-success" onclick="changeStatus('status-<%=order.order_id%>', 'Success')">
                                    <i class="fas fa-check"></i> Success
                                </a>
                                <a href="#" class="btn-failed" onclick="changeStatus('status-<%=order.order_id%>', 'Failed')">
                                    <i class="fas fa-times"></i> Failed
                                </a>
                            </div>
                        </td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
    
    <!-- Image Modal -->
    <div id="imageModal" class="modal">
        <div class="modal-content">
            <span class="close-modal">&times;</span>
            <img id="billImage" src="" alt="Bill Image">
        </div>
    </div>
    
    <script src="/Views/Packing/packing.js"></script>
</body>

</html>